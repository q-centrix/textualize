require 'airborne'

describe 'DELETE <%= url %>' do
  let!(:<%= name %>) { create(:<%= name %>) }

  <%= secured_by %>.each do |role|
    it "returns expected json for #{role} role" do
      add_header_token role
      delete "<%= url %>"
      expect_status <%= response_code %>
    end
  end
end

