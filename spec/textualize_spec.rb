require 'spec_helper'

describe Textualize do
  it 'has a version number' do
    expect(Textualize::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
